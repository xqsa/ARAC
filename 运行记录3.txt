第三次启动又退出，查状态：

**24/24 cell 全部完成**——退出码 1 是判定结果而非崩溃。读完整判定：

## Gate 53 判定：契约全过、两条实质条款未过——按预注册失败语义收案

**24/24 cell 完成零违例**，判定结果（诚实且信息量大）：

| 条款 | 结果 |
|---|---|
| S1 无税 | ✅ 完美（shared=0 → 仲裁静默、FE=0、ratio=1.0000） |
| 全局非劣 | ✅（最大 ratio 1.0043） |
| terminal 精确 | ✅ 两臂 24/24 |
| 高重叠严格更优 | ❌ 无一 case B<A |
| 单调趋势 | ❌ Spearman 0.14, p=0.46 |

机理清楚：仲裁车道 18 周期 × 3 FE 全部执行但**零接受**——σ=0.05·span 的块扰动代理提案对已收敛 incumbent 差 1–2 个量级（冒烟时的观察在全网格复现）。Gate 29/30 的 36/36 胜利靠的是 SMP sense 供给的**信息性**提案；派发路径没有提案源，廉价代理无法替代。B ≈ A − 54 FE，ratio 全部 ≥1.0。

按协议失败语义：**仲裁按 case 关闭，主线 = 纯派发**；不做 gate 内调参重试。记录后执行第二刀：

第二刀范围比预期更干净：src 内**唯一**导入 episodes 的是 overlap_core.py。查两个 `__init__` 的再导出后执行：

执行第二刀。先改 overlap_core.py（去 import、v5 分支、三个具名入口）：

还有引用点，全面扫一遍再清：

`test_oc_unified_loop.py` 本身保留（第三刀范围），但它里面两个专测 v5 退役 raise 的用例和 import 随机制一起过时，精确移除：

这两个测试文件测的是 gate51c 实验机制（属调度线，其宿主模块 import episodes 已不可用）。按清单删除后重跑：

583 过 7 挂。区分是我的删除导致还是既有失败：

3 个确认既有。快速验证其余 4 个也与删除无关：

7/7 全部为**既有失败**（stash 前后逐位一致，均为对仓库状态敏感的审计型测试，与调度删除无关）。跑 ruff 后提交：

仓库 venv 里有 ruff，直接跑：

ruff 全绿。提交第二刀：

第二刀落地（a571192）。最后做 #7 论文素材整理：

## 收尾总纲执行完毕：#1–#7 全部落地，工作树干净

**本轮完成的三件事：**

**Gate 53 判定收案（#4）**：24/24 cell 零违例。契约层全过（S1 无税完美成立：shared=0 → 静默、ratio=1.0000；两臂 terminal 精确 3M），但两条实质条款未过——**仲裁车道 216 FE/case 全部执行而零接受**，σ=0.05·span 的块扰动代理提案对收敛 incumbent 差 1–2 个量级。按预注册失败语义：仲裁按 case 关闭，主线 = 纯派发。最有价值的副产品是**发现谱曲线**：共享变量 0/18/51/80/119/130 对 ov 0/1/3/5/7/10，跨 seed 完全稳定、ov0 零假阳性——直接可进论文。

**代码第二刀（#6）**：commit `a571192`——按预注册清单删除 `episodes.py` 全部 2,852 行 v4–v5.3 调度机制 + 6 个调度线测试，剥离 overlap_core/包入口的 v5 分支。回归验证：pytest 583 通过；7 个失败经 stash 前后**逐位比对**确认全部既有（审计型测试，与删除无关，证据写入提交信息）；ruff 全绿。历史可运行状态完整保存在 tag `v5.3-prealation`。

**论文素材（#7）**：`docs/arac-oc-paper-materials.md`——六条主张各带 gate 证据编号、分析节五条素材（跷跷板刻画/双轴根因/象限稳定性/调度信号三段闭案/SACC 预算敏感）、五条诚实边界（Gate 53 的不可移植性是第一条）、图表数据源映射。

**收尾总纲 §10 的宣言至此兑现**：“代谢产物进博物馆（tag、frozen artifacts、分析节），生产车间只留承重墙：发现、派发、仲裁。”——其中仲裁的价值边界由 Gate 53 精确圈定（循环语境成立，代理提案不可移植）。剩余 #8（第三刀 + 论文本体写作）和三个可选后续（Gate 53b 信息性提案源、v6.0 Gate 52、G8/G9 晋级 gate）已登记，随时可接。