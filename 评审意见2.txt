这份文献对照报告质量很高——“双轴状态”诊断与我们刚闭合的 G4/G5/G10 证据完全吻合（v5 层同时携带 R6 收益与 S5 灾难 = 单轴规则区分不了“难度×贡献”两个维度）。我按 §三 的顺序执行：可当场完成的先做（引用缺口、**双轴表示的回放验证**——这是 §三.2 明确要求“先验证再实现”的步骤），然后落 v6.0 预注册。

先看 related work 落点和 receipt 数据结构：

文献库和 receipt 数据都齐了。开始执行：先补 §三.1 的引用缺口文档，然后做 §三.2 要求的**双轴表示回放验证**（先验证、后实现——这是路线图自己的纪律）：

写双轴回放 gate（G11）：

S5 的象限矩阵完美吻合文献假设（CTP=difficult/contributing，模拟分配给 CTP 57%），但总判定没过——看是 P2 还是 P3 挂了：

预注册判定如实记录：**表示"按本规范"不充分**——P1/P3（保护方向）3/3 通过，但 P2 失败暴露了真问题：基于"改进到达间隔"的难度代理**无法区分"平坦因为易且已榨干"和"平坦因为未显现"**——恰好是路线图最需要的释放/供血判据。查一个明确标注为事后探索的改进方向：receipt 里的 local progress 能否分开这两类：

重要发现——重新对照路线图自己的象限标注：R2 的 aor 被分到 difficult/flat（**供血等待**象限），这**正是路线图给 R2-AOR 指定的位置**（"R2-AOR 在右下→供血"）！我的 P2 把"R2 的平坦臂"操作化成了"存在 easy+flat episode"，但路线图里 R2 的平坦臂**就是** AOR 且应落在供血象限——这是预注册歧义，不是表示失败。查 R6 象限补全证据：

R6 的象限也吻合（aor/ctp = easy/contributing → 正常 exploit，正是 v5 层唯一赢 R6 的方式）；R2/A3 跨 seed 象限不稳定——与"两层调度器都赢不了它们"一致。证据链齐了，写 v6.0 预注册设计文档：

## 文献路线图已评审并执行完当前可完成的全部三项

**我的评审意见**：这份报告的诊断质量很高——“一个根因（单轴状态机）+ 四个症状”的框架与我们刚闭合的 G4c/G5 证据精确互锁（v5 层同时携带 R6 收益与 S5 灾难 = 单轴规则无法区分难度×贡献两个维度；ladder/grace 正是单轴框架里模拟第二轴的补丁）。文献映射是机制级的且诚实（§四明确了 FDC 成本、disjoint 分解差异、RL 与审计文化的冲突）。唯一需要修正的地方在执行中发现了：**路线图对 R2-AOR 的象限标注自相矛盾**（同时标“平坦臂→快释放”和“AOR→供血”），而数据给出了裁决。

### 本轮完成的执行

**1. 引用缺口（§三.1 剩余项）** → `docs/arac-oc-related-work-gaps.md`：FEA（完整目标仲裁的直接先例，此前未引）、CBCC-overlapping、OCC、HCC 定位叙事（引用其开放问题原文 + "oracle 上界 vs 黑盒可达”）、AOS extreme-credit lineage（为 strict-best archive 背书）。

**2. 双轴表示回放验证（§三.2 要求的“先验证后实现”）** → G11 gate，结果是本轮最重要的新知识：

- **保护半边验证成立（3/3）**：S5-CTP 全 seed 落 `difficult/contributing` 且贡献恒第一；两轴分配规则给 CTP 模拟份额 **57.1%**——正确落在获胜 off 臂（62-66%）与 v5.3 饥饿份额（20.6%）之间。S5-AOR 落释放象限，R6 的 aor/ctp 落正常 exploit 象限（与 v5 层唯一获胜模式一致）。
- **喂血/释放判别是真正的开放问题**：间隔式难度代理对零全局增益的 episode 无判别力（R2-AOR 按数据落在供血象限——这其实与路线图给 AOR 的本意一致，但意味着“快释放”判据需要窗口级贡献衰减信号，不能用到达间隔）。
- **象限稳定性本身就是信号**：S5 放置跨 seed 完全稳定（所以政策清晰）、R2/A3 不稳定（所以谁也赢不了它们）。

**3. v6.0 设计预注册** → `docs/arac-oc-v6_0-design.md`：双轴连续状态（rank credit + **窗口级衰减斜率**——把 G11 的教训写成硬性约束：喂血/释放不得用间隔代理）、CCFR3 衰减中断替代 w1 ladder+grace、UCB 替代三车道、单一两输入分配函数（净减机制）；Gate 52 六条通过标准，含 **fresh-seed 象限复现**（已验证半边必须在未见数据上复现）和**机制数净减**写入 confirmation。

### 未执行与原因

**v6.0 实现 + Gate 52 campaign** 是下一 program 的第一项——它要替换 v4 引擎内的 material/ladder/grace/三车道机制（约千行级改动），且 Gate 52 是又一层完整 12-cell 网格。按路线图自己的纪律（先验证后实现、减法先行），本轮把它停在“设计已冻结、实现已解锁”的边界上是正确的。SACC phase-2 同理排在 v6.0 之后。所有状态已写入 SUBTASKS（新增 14/15 两行 pending），随时可以接续。