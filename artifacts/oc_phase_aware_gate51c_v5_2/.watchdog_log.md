# Watchdog log

- 2026-08-19 22:39 (cron #1): 发现 gate 主进程消失（8+1 个 OC cell 已完成、
  日志 275 字节、无 traceback；Windows 系统日志无睡眠/关机/崩溃事件，
  无 python 应用错误——判定为宿主环境静默终止后台任务）。
  对策：改用 PowerShell Start-Process OS 级脱离重启（stdout/stderr 重定向
  至本目录 run_stdout.log / run_stderr.log），15 个缺失 cell 续跑。
  同时修正看护自动化的进程检查命令（原 Where-Object $_ 写法在 Git Bash
  双引号下会被展开损坏，恒返回空 → 会误判进程死亡反复重启）。
- 2026-08-19 23:12 (cron #1 续): 看护 prompt 的 4a 进程检查命令在 Git Bash 下
  必然损坏（$_ 被 bash 展开）——已把正确的判活命令（Get-Process python 计数）
  与脱离式重启命令写入 .watchdog_state.json 的 instructions 字段。下次唤醒
  请先读该文件。任何"判死重启"决定必须以正确命令的计数=0 为准。
- 2026-08-20 01:06-01:15 (cron #4): 完成态确认——72/72、failures=[]、confirmation
  落盘。gate 进程干净退出（无孤儿）。晨报 docs/morning-report-2026-08-20.md 已写，
  进度日志已补判定条目，判定产物已提交（d94b9bf）。自删调用遇客户端工具故障
  未执行——完成态下后续唤醒为无操作，请用户手动删除 automation-68051d78。
